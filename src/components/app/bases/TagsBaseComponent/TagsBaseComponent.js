import React from 'react';
import PropTypes from 'prop-types';
import styles from './TagsBaseComponent.module.css';

const TagsBaseComponent = () => (
  <div className={styles.TagsBaseComponent}>
    TagsBaseComponent Component
  </div>
);

TagsBaseComponent.propTypes = {};

TagsBaseComponent.defaultProps = {};

export default TagsBaseComponent;
